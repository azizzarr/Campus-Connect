import { FournisseurFilterPipePipe } from './fournisseur-filter-pipe.pipe';

describe('FournisseurFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FournisseurFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
